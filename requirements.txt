##########################
# Core ML
##########################
scikit-learn>=1.3
joblib>=1.3

##########################
# Web API
##########################
Flask>=3.0
flask-cors>=4.0
flasgger>=0.9.11

##########################
# NLP / Text
##########################
spacy>=3.7

##########################
# PDF & Reporting
##########################
pdfplumber>=0.11
reportlab>=4.0

##########################
# Visualization / Graph
##########################
networkx>=3.2
matplotlib>=3.8

##########################
# Data / Stats
##########################
pandas>=2.1
numpy>=1.26

##########################
# Semantic Search (optional dense embeddings)
##########################
sentence-transformers>=2.2  # optional; falls back to TF-IDF if not installed
transformers>=4.40  # for advanced transformer-based suspect classifier (option A)
torch>=2.2  # CPU version is fine; enables DistilBERT fine-tuning

##########################
# Auth / Security
##########################
PyJWT>=2.9
passlib>=1.7  # optional hashing utilities

##########################
# Scheduling / Monitoring
##########################
APScheduler>=3.10
prometheus-client>=0.20

##########################
# Optional DB connectors (only if you actually use Postgres/MySQL)
##########################
psycopg2-binary>=2.9 ; platform_system != 'Windows' or platform_machine != 'ARM64'
mysql-connector-python>=8.3

##########################
# Dev / Utilities (optional)
##########################
python-dotenv>=1.0

# After install run once:  python -m spacy download en_core_web_sm
